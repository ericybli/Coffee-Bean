# macOS
.DS_Store

# Local build artifacts (IPA output)
dist/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
xcuserdata/
DerivedData/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# The Xcode project IS committed (Xcode Cloud needs it in the repo).
# It is still generated from App/project.yml — after editing project.yml run
# `cd App && xcodegen generate` and commit the result.
